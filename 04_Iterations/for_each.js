
const coding = ["java", "Javascript" , "Python" , "C++"]

coding.forEach(function(language,index){ //first parameter is item , index and arr are available as well
    console.log(language,index);
});