const alphabet =['A','B','C','D'];
const numbers =[1,2,3,4]

const [first,second,,fourth]= alphabet; // skipped third element

console.log(`first element in alphabet array is ${first} and last one is ${fourth} `); // no need to always access first element of array using index now

